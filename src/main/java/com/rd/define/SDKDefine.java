package com.rd.define;

import java.util.concurrent.TimeUnit;

public class SDKDefine {

    public final static int SHARE_REWARD_INTERVAL = (int) (120 * TimeUnit.MINUTES.toSeconds(1));

    public final static byte SHARE_DAILY_REWARD_TIMES = 3;
}
